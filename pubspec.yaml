name: glass_calendar_pro
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Các thư viện đã sử dụng trong code main.dart
  cupertino_icons: ^1.0.2
  table_calendar: ^3.0.9
  intl: ^0.18.1
  vnlunar: ^1.1.0
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^16.0.0
  timezone: ^0.9.2 # Hỗ trợ tính toán múi giờ cho thông báo

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  # Cấu hình icon launcher (nếu có file ảnh)
  # assets:
  #   - images/